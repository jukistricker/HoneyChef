using AutoMapper;

namespace IOITCore.Mappings
{
    public class AutoMapping : Profile
    {
        public AutoMapping()
        {
            //CreateMap(typeof(IPagedResult<>), typeof(IPagedResult<>)).ReverseMap();
        }
    }
}
